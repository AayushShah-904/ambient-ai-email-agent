# This file is used by Streamlit Community Cloud to inject secrets as env vars.
# Go to your app dashboard → Settings → Secrets and paste this content there.
# DO NOT commit the real secrets.toml — only commit this example file.

BACKEND_URL = "https://your-render-app.onrender.com"
PUBLIC_BACKEND_URL = "https://your-render-app.onrender.com"
